{
  "name": "kfu-raspisanie-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Бэкенд расписания КФУ: воркер читает таблицу, БД хранит, API отдаёт приложению.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --experimental-sqlite src/index.js",
    "dev": "node --watch --experimental-sqlite src/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.1"
  }
}
