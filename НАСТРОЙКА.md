# Расписание КФУ — своя страница + уведомления в Telegram

## ✅ Уже настроено (7 сентября 2026)

**Ссылка на расписание:**
`https://script.google.com/macros/s/AKfycbw3Q_91thWAajWY1YVjOSrBrDA4tzjfmCYN7W3x4NSV_cGo-u4VF-Z5dvjjUXJFiRGg/exec`

Проект: [Расписание](https://script.google.com/home/projects/1Qvp7WZver8iojFvmhEJGc-0foC03lvUUKT3zPJP4UnZWh3Z1KcuN3rhf/edit) в Apps Script.
Шаги 1–3, 6, 7 ниже выполнены: код залит, часовой пояс Ташкент, почасовой триггер работает,
веб-приложение развёрнуто (версия 4, доступ «Only myself»).

Сейчас в приложении выбрана группа **Автоматизация 15.03.04** — я ставил её для проверки.
Нажми «сменить» вверху и выбери свою.

**Осталось только Telegram — шаги 4 и 5.**

---

Инструкция ниже — на случай, если понадобится пересобрать всё с нуля.
Дальше всё работает само: страница открывается по ссылке с телефона,
а бот пишет, когда в таблице что-то поменяли.

**Что где лежит:**

| Файл | Куда |
|---|---|
| `apps-script/Code.gs` | в проект Apps Script, файл `Код.gs` |
| `apps-script/Index.html` | в проект Apps Script, HTML-файл с именем `Index` |
| `apps-script/appsscript.json` | справочно (часовой пояс) |
| `apps-script/_preview/` | только для локального просмотра вёрстки, копировать не нужно |

---

## Шаг 1. Создать проект

1. Открой **[script.google.com](https://script.google.com)** под тем же аккаунтом, с которого
   у тебя открывается таблица расписания.
2. **Новый проект** → вверху нажми «Проект без названия» → назови `Расписание`.

## Шаг 2. Вставить код

1. В файле `Код.gs` выдели всё (Ctrl+A) и вставь содержимое **`apps-script/Code.gs`**.
2. Слева у надписи «Файлы» нажми **+** → **HTML**. Имя файла — ровно `Index`
   (расширение `.html` Google добавит сам).
3. Выдели всё в новом файле и вставь содержимое **`apps-script/Index.html`**.
4. Сохрани (Ctrl+S).

## Шаг 3. Часовой пояс

Слева **⚙ Настройки проекта** → **Часовой пояс** → `(GMT+05:00) Ташкент`.

Без этого «сейчас идёт пара» будет считаться по чужому времени.

## Шаг 4. Telegram-бот

1. В Telegram открой **[@BotFather](https://t.me/BotFather)** → команда `/newbot`.
2. Придумай имя (любое) и username (должен заканчиваться на `bot`, например `kfu_raspisanie_kam_bot`).
3. BotFather пришлёт строку вида `8123456789:AAH...` — это токен. **Никому его не показывай.**
4. В Apps Script: **⚙ Настройки проекта** → внизу **Свойства скрипта** → **Добавить свойство**:
   - Свойство: `TG_TOKEN`
   - Значение: твой токен
   - **Сохранить свойства скрипта**
5. Вернись в Telegram, открой своего бота и нажми **Start** (или напиши ему любое сообщение).

## Шаг 5. Привязать чат и разрешить доступ

1. В редакторе кода вверху в списке функций выбери **`tgFindChatId`** → **Выполнить**.
2. Google попросит доступ:
   **Проверить доступ** → выбери свой аккаунт → «Приложение не проверено» →
   **Дополнительные настройки** → **Перейти на страницу «Расписание» (небезопасно)** → **Разрешить**.
   Это нормально: проект твой, «непроверенный» — потому что он не публиковался в Google.
3. В Telegram от бота придёт сообщение **«✅ Готово. Бот подключён»**.

## Шаг 6. Включить почасовую проверку

Выбери функцию **`setupTrigger`** → **Выполнить**.

Скрипт создаст триггер «раз в час» и сделает первый снимок расписания.
Дальше он сам сравнивает таблицу со снимком и пишет только когда что-то реально изменилось.

## Шаг 7. Получить ссылку на страницу

1. Справа вверху **Развернуть** → **Новое развёртывание**.
2. Шестерёнка рядом с «Тип» → **Веб-приложение**.
3. Заполни:
   - Описание: `Расписание`
   - Запуск от имени: **Я**
   - У кого есть доступ: **Только я**
4. **Развернуть** → скопируй **URL веб-приложения** (заканчивается на `/exec`).

> **Про доступ.** «Только я» — ссылка работает лишь на устройствах, где ты вошёл в этот
> Google-аккаунт. Если на телефоне не заходишь в Google, поставь «Все» — тогда ссылка
> откроется у любого, кто её знает (расписание и так общедоступно внутри вуза, но решай сам).

5. *(необязательно)* Добавь свойство скрипта `WEBAPP_URL` = этот URL — тогда в сообщениях
   бота появится кнопка «Открыть расписание».

## Шаг 8. Настроить под себя

Открой ссылку на телефоне:

1. Нажми **«сменить»** вверху справа → найди свою группу (есть поиск) → выбери.
2. Нажми **«Уведомления выкл»** — переключится на «вкл», и бот будет писать про изменения
   именно твоей группы.
3. Меню браузера → **«Добавить на главный экран»** — появится иконка как у обычного приложения.

---

## Что ты получишь

**Штрихкод дня** — шесть сегментов по числу пар. Занятые залиты цветом предмета, свободные
пустые. Один взгляд — и видно, во сколько начало, где окна и когда конец. Текущая пара
обведена рамкой и заливается сверху вниз по мере того, как идёт.

**Цвет закреплён за предметом**, а не за статусом: матанализ всю неделю одного цвета.
Поэтому в «Неделе» рядом с каждым днём стоит мини-штрихкод — неделя читается без чтения.

**Вкладка «Сегодня»** — сверху крупно то, что нужно прямо сейчас: идущая пара с обратным
отсчётом, или следующая с временем до неё, или первая пара завтра. Ниже — весь день
с окнами («окно 1 ч 30 мин») и завтрашний день.

**Вкладка «Неделя»** — шесть дней подряд, с количеством пар на неделе в подписи.

**Вкладка «Правки»** — журнал: когда заметили правку, что было (зачёркнуто) и что стало.
Изменённые пары помечаются прямо в расписании строкой «было: …».

**В Telegram** — сообщение вида:

```
📅 Расписание изменилось

1 курс Информационные системы и технологии 09.03.02
• Среда, 2-я пара (10:00 – 11:20)
   было: Химия 301 ауд. Рузматов И.Р. проф.
   стало: Физика 205 ауд. доц.Маматкулов Б.Х.
```

---

## Если что-то не так

**Бот молчит.** В Apps Script слева **Триггеры** — там должен быть `checkChanges`, «Каждый час».
Рядом в **Журналы выполнений** видно, отработал ли он и с какой ошибкой.

**«Не вижу сообщений» при `tgFindChatId`.** Ты не нажал Start у своего бота — напиши ему
что угодно и запусти функцию заново.

**Страница пустая / ошибка загрузки.** Проверь, что HTML-файл называется именно `Index`,
и что твой аккаунт открывает исходную таблицу.

**Изменил код — на странице по-старому.** Каждое изменение нужно передеплоить:
**Развернуть → Управление развёртываниями → ✏️ → Версия: «Новая версия» → Развернуть**.
Ссылка при этом не меняется.

**Расписание на странице отстаёт.** Данные кэшируются на 10 минут — нажми **«Сверить сейчас»**
вверху, это перечитывает таблицу сразу.

**Вуз выложил расписание в новый файл.** Тогда поменяй `SOURCE_ID` в начале `Code.gs`
на ID новой таблицы (это кусок ссылки между `/d/` и `/edit`) и передеплой.

---

## Разовые проверки, которые можно запускать вручную

| Функция | Что делает |
|---|---|
| `setupTrigger` | пересоздаёт почасовой триггер |
| `checkChanges` | проверить прямо сейчас |
| `tgFindChatId` | заново привязать Telegram |

## Где скрипт хранит данные

В твоём Google Диске появится папка **«Расписание КФУ — данные»** с двумя файлами:
`raspisanie_snapshot.json` (последний снимок) и `raspisanie_changes.json` (история изменений).
Удалять их не нужно; если удалишь снимок — следующая проверка просто начнёт историю заново.
