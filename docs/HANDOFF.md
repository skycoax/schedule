# Para — где мы остановились (25 сентября 2026)

Записка для следующей сессии (в т. ч. облачной). Правила работы — в `AGENTS.md`.

## Что работает на проде (para.skycoax.uz)

- Расписание всех 14 вузов, без интернета (service worker), режим преподавателя.
- Совместные пары: «Вместе с N группами» (`server/src/together.js`).
- Новая навигация: нижняя панель вкладок Расписание / Обсуждения / Профиль; вверху «Сегодня | Неделя»,
  «Правки» — круглой кнопкой справа (лист).
- Аккаунты: вход через Google (OAuth code + PKCE), гость по умолчанию, возраст ≥ 16 (`SOCIAL_MIN_AGE`).
- «Обсуждения» вуза: посты (до 4 фото), ветки ответов (1 фото), лайки, категории, жалобы, блокировки,
  модерация (модератор — kamolov1575@gmail.com), маскировка мата, лимиты, аварийный `SOCIAL_MODE`.
- Профиль: аватар, @имя, «о себе», Telegram/Instagram, друзья, поиск людей, удаление аккаунта.
- Страницы: `/policy`, `/rules`, `/delete-account` (разработчик «skycoax», данные в Узбекистане, Ташкент) — на всех адресах.
- Адреса вузов (kfu.skycoax.uz, tsue.skycoax.uz…) — та же Para с выбранным вузом: вкладки, вход, «Обсуждения»
  (вход через Google идёт через Para и возвращается на адрес вуза, см. раздел «Обсуждения» в `AGENTS.md`).
- iPhone (iOS 26–27): ошибка WebKit сдвигает закреплённые элементы после клавиатуры — поправка `--vv-fix`
  в `web/src/ui/keyboard.ts`; проверить на iPhone после входа через Google и набора текста.
- Контракт соцчасти: `docs/social/CONTRACT.md`. Код: `server/src/social/`, `web/src/social/`, `web/src/shell/`, `web/src/ui/`.

## Google Cloud (готово)

Проект «Para» (`para-509712`), экран входа опубликован (In production), клиент «Para web» с redirect
`https://para.skycoax.uz/api/auth/google/callback`. Ключи лежат только в `server/.env` на VPS
(`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SOCIAL_ADMIN_EMAILS`, `SOCIAL_SALT`) — в репозитории их нет и быть не должно.
Владелец проверил вход вживую — работает.

## Что осталось

### 1. Google Play (главное)
Приложение «Para — расписание вузов» (`uz.skycoax.para`) создано в Play Console (личный аккаунт
kamolov1575@gmail.com), но ещё НЕ отправлено. Заполнено до появления аккаунтов — теперь надо обновить:
- «Безопасность данных»: добавились аккаунты (Google ID, почта, имя), фото, посты/ответы, друзья, жалобы;
  создание аккаунта через OAuth; URL удаления `https://para.skycoax.uz/delete-account`.
- Анкету возрастного рейтинга — пройти заново (пользователи общаются и делятся фото, модерация есть).
- Целевой возраст: убрать 13–15, оставить 16–17 и 18+ (аккаунты с 16).
- Готовые ответы и тексты — `android/play-listing.md`.
- Загрузить картинки из `android/play/` (иконка, баннер, 4 скриншота телефона, по 4 для планшетов 7" и 10")
  и `app-release.aab` (сборка — `android/README.md`), затем взять SHA-256 ключа подписи Google
  (Целостность приложения) и добавить ВТОРЫМ отпечатком в `server/hub/assetlinks.json`, выложить.
- Закрытый тест: 12 тестировщиков 14 дней подряд (личный аккаунт разработчика), потом заявка на прод.

### 2. Резервные копии
`jobs.js` хранит 7 ежедневных копий `social.db` в `data/backup/` на том же диске; фото (`data/media/`)
не копируются, базы расписаний тоже. Нужна копия вне сервера (другой диск/облако) — по решению владельца.

### 3. Мелочи из ревью (не срочно)
Офлайн-старт требует хотя бы одного прошлого визита под service worker; в режиме преподавателя «Назад» на
первом выборе преподавателя закрывает приложение; упоминание удалённого аккаунта остаётся в тексте ответа.

## Ограничения облачной сессии

- SSH-ключа к VPS в облаке нет → выкладку (`DEPLOY_HOST=skycoax-vps bash deploy/deploy.sh`) и всё на сервере
  делает владелец со своего компьютера (или локальная сессия Claude).
- Play Console и Google Cloud — только через браузер владельца; пароли и коды вводит сам владелец.
- Проверка перед выкладкой: `cd web && npx tsc --noEmit && npm run build`,
  `for f in server/src/*.js server/src/social/*.js; do node --check "$f"; done`,
  дымовой тест соцчасти — раздел «Обсуждения» в `AGENTS.md`.
