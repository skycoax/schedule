// Полный текст условий и данных внутри приложения (на «ты»). Открывается поверх согласия, из подвала,
// из «Профиля» и из окна входа — поэтому без id: на странице может быть несколько копий (.sheet--doc).
// Всё, что отличается у вузов (название, источник, эмблема), — из brand.ts. Раздел про аккаунт и «Обсуждения» —
// там, где они есть (hasSocial: Para и адреса вузов). Полная политика — server/hub/policy.html (/policy на любом адресе).
// Факты здесь и там должны совпадать (CONTRACT.md §E.6). Дату меняем вместе с policy.html.
import { useEffect, useId, useRef, type CSSProperties, type KeyboardEvent } from 'react';
import { brand, hasSocial } from '../brand';

// На адресе Para в шапке знак Para, а эмблема вуза видна только в списке вузов (tenant.json пишет «в шапке»).
const logoCredit = brand.hub ? brand.logoCredit.replace(' в шапке ', ' в списке вузов ') : brand.logoCredit;

// «Закрыть» — общий .hdr__btn: он рисуется высотой 34 px, а область нажатия 44 px добирает его ::after (index.css).
const listLink: CSSProperties = { display: 'inline-flex', alignItems: 'center', minHeight: 44 };

export function DocSheet({ open, onClose }: { open: boolean; onClose: () => void }) {
  const titleId = useId();
  const ref = useRef<HTMLDivElement>(null);
  const onCloseRef = useRef(onClose);
  onCloseRef.current = onClose;

  // Фокус — внутрь листа, при закрытии — обратно туда, откуда открыли.
  useEffect(() => {
    if (!open) return;
    const opener = document.activeElement as HTMLElement | null;
    ref.current?.focus({ preventScroll: true });
    return () => {
      if (opener && opener.isConnected && typeof opener.focus === 'function') {
        try { opener.focus({ preventScroll: true }); } catch { /* элемент уже не в документе */ }
      }
    };
  }, [open]);

  // Esc закрывает только этот лист, а не окно под ним (лист может лежать внутри другого листа).
  const onKeyDown = (e: KeyboardEvent<HTMLDivElement>) => {
    if (e.key !== 'Escape') return;
    e.stopPropagation();
    onCloseRef.current();
  };

  return (
    <div
      ref={ref} className={'sheet sheet--doc' + (open ? ' open' : '')}
      role="dialog" aria-modal="true" aria-labelledby={titleId} tabIndex={-1} onKeyDown={onKeyDown}
    >
      <div className="sheet__bar">
        <h2 className="sheet__h" id={titleId} style={{ flex: 1 }}>Условия и данные</h2>
        <button type="button" className="hdr__btn" onClick={onClose}>Закрыть</button>
      </div>
      <div className="sheet__list doc">
        <p className="doc__upd">Обновлено 26 сентября 2026</p>

        <h3>Что это</h3>
        <p>{brand.about}</p>
        {logoCredit && <p>{logoCredit}</p>}

        <h3>Откуда данные</h3>
        <p>{brand.source}</p>

        <h3>Без гарантий</h3>
        <p>Расписание может отставать, содержать ошибки вуза или не учитывать устные
          изменения. Перед важным решением сверяйся с официальным расписанием и деканатом.
          Автор не отвечает за пропущенные пары, опоздания и любые последствия
          использования приложения.</p>

        {hasSocial && <>
          <h3>Аккаунт и «Обсуждения»</h3>
          <p>Аккаунт — по желанию, с 16 лет, вход через Google. Посты, ответы, фото, имя и фото профиля
            видят все, кто открывает «Обсуждения». Почту другие люди не видят. Удалить аккаунт — «Профиль» →
            «Удалить аккаунт».</p>
          <ul>
            <li>От Google Para берёт Google ID, почту, имя и фамилию, фото, язык аккаунта и домен
              организации (если это почта вуза). Имя заполняет профиль, а фото сразу становится фото
              профиля — его можно поменять или убрать. Контакты, письма и файлы Google не берём,
              скриптов Google в приложении нет.</li>
            <li>Ещё Para записывает, где и с какого устройства заведён аккаунт и когда ты входил
              («iPhone · Safari», без модели и версий).</li>
            <li>Почту, возраст, данные Google, устройства и входы видят только разработчик и модераторы —
              чтобы разбирать нарушения. Google ID не видит никто.</li>
            <li>Аккаунт — с 16 лет: нажимая «Продолжить с Google», ты это подтверждаешь.</li>
            <li>Профиль, «О себе» и «мой вуз» видят только те, кто вошёл. Список друзей и блокировок, кто
              поставил отметку и кто пожаловался, не видит никто.</li>
            <li>Фото перед отправкой уменьшаются, а сервер удаляет из них место съёмки и модель телефона.
              Удалённые фото стираются сразу, но до 7 дней могут оставаться в кеше браузера у тех,
              кто их видел.</li>
            <li>Моменты — фото с камеры на сутки: по умолчанию их видят все, кто вошёл, в «Обсуждениях» твоего вуза,
              а можно выбрать «Только друзья». У тебя они хранятся год в «Твоих моментах». Видео с камеры никуда
              не уходит — только сделанный снимок.</li>
            <li>Личных сообщений нет. Грубые слова скрываются автоматически. На пост, ответ или профиль
              можно пожаловаться, а человека — заблокировать; жалобы разбирают модераторы.</li>
          </ul>
        </>}

        <h3>Что хранится в твоём телефоне</h3>
        <ul>
          {brand.hub && <li><b>uni</b> — выбранный вуз;</li>}
          <li><b>group</b> — выбранная группа;</li>
          <li><b>role</b> и <b>teacher</b> — студент ты или преподаватель и выбранный преподаватель;</li>
          {brand.hub && <li><b>prev_…</b> — группа и преподаватель в других вузах, чтобы вернуть их,
            когда вернёшься к тому вузу;</li>}
          <li><b>seenTs</b> — метка последней просмотренной правки;</li>
          <li><b>agreed</b> — что ты принял эти условия;</li>
          <li><b>theme</b> — выбранная тема оформления;</li>
          <li><b>cid</b> — случайный номер для подсчёта заходов, ни с чем не связанный;</li>
          <li><b>homeShown</b>, <b>reviewPromptAt</b>, <b>reviewedAt</b>, <b>myRating</b> — показывали ли
            подсказку «На главный экран», когда предлагали оценить приложение и какую оценку ты поставил{hasSocial ? ';' : '.'}</li>
          {hasSocial && <>
            <li><b>tab</b> и <b>tabAt</b> — последняя открытая вкладка и когда её открывали;</li>
            <li><b>me_cache</b> — копия твоего имени, @имени и фото, чтобы показать их сразу;</li>
            <li><b>draft_post_&lt;вуз&gt;</b> — черновик поста: текст, тема и загруженные фото;</li>
            <li><b>hiddenUsers</b> — авторы, чьи посты ты скрыл: номер, @имя и имя;</li>
            <li><b>ageBlock</b> — что вход пока недоступен по возрасту, хранится 30 дней;</li>
            <li><b>coach_chat</b> — показывали ли подсказку про «Обсуждения»;</li>
            <li><b>chatCat</b> — выбранная тема ленты, до закрытия приложения;</li>
            <li><b>__Host-para_sid</b> — ключ входа в аккаунт (кука, недоступная скриптам): есть, только
              пока ты вошёл, действует 180 дней без входа и удаляется при выходе;</li>
            <li><b>__Host-para_oauth</b> — одноразовая метка на 10 минут, пока идёт вход через Google.</li>
          </>}
        </ul>
        <p>Это технические записи для работы приложения. Рекламных и отслеживающих кук нет.
          Чтобы расписание открывалось без интернета, телефон хранит последнюю загруженную
          версию расписания — она никуда не отправляется.
          {hasSocial && ' «Обсуждения» и профили специально в телефоне не сохраняются.'}</p>

        <h3>Аналитика</h3>
        <p>Мы собираем обезличенную статистику посещений — чтобы понимать, сколько
          людей пользуется расписанием и откуда они приходят. Имя, телефон, почта
          и точное местоположение в ней не собираются, IP-адрес в статистике не хранится —
          только необратимый отпечаток для защиты от спама. Данные не передаются третьим лицам
          и хранятся на сервере автора в Узбекистане. Заход записывается только <b>после того,
          как ты принял эти условия</b>.</p>
        {hasSocial && <p>Сервер не связывает номер cid с аккаунтом: код обсуждений его не читает и не хранит.</p>}

        <h3>Отзывы</h3>
        <p>Отзыв и оценка видны всем — без пароля и без входа. Имя можно не указывать
          (по умолчанию «Аноним»), а сам номер браузера (<b>cid</b>) используется только
          для защиты от спама — не чаще одного отзыва в 10 минут с одного устройства.</p>

        <h3>Персональные данные</h3>
        <p>{hasSocial
          ? 'Без аккаунта Para не собирает имя, почту, контакты и точное местоположение. Данные аккаунта не продаются, не передаются третьим лицам и не используются для рекламы.'
          : 'Приложение не собирает имя, контакты и точное местоположение. Обезличенная статистика используется только в сводном виде.'}</p>

        <h3>Изменения</h3>
        <p>Условия могут меняться, дата обновления указана вверху.</p>

        <h3>Подробнее</h3>
        <ul>
          <li><a href="/policy" target="_blank" rel="noopener" style={listLink}>Полная политика конфиденциальности</a></li>
          {hasSocial && <li><a href="/rules" target="_blank" rel="noopener" style={listLink}>Правила обсуждений</a></li>}
        </ul>

        <h3>Связь</h3>
        <p><a href="https://t.me/skycoax" target="_blank" rel="noopener">@skycoax</a> в Telegram
          или <a href="mailto:kamolov1575@gmail.com">kamolov1575@gmail.com</a>.</p>
      </div>
    </div>
  );
}
